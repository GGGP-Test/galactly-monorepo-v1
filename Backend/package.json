{
  "name": "buyers-api",
  "version": "1.0.0",
  "private": true,
  "description": "Leads/Buyers API (Node 20, ESM, tsx runtime – no build step)",
  "type": "module",
  "engines": { "node": ">=20" },

  "scripts": {
    "start": "node --enable-source-maps ./node_modules/tsx/dist/cli.js src/index.ts",
    "dev": "tsx watch src/index.ts",
    "lint": "echo \"no lint configured\""
  },

  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "tsx": "^4.19.1"
  }
}