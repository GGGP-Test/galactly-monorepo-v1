// Dependency-free placeholder so TypeScript compiles without 'express' or 'stripe' packages.
// If you later add Stripe, replace this with your real handlers.

export function registerStripeHandlers(): void {
  // no-op
}
