# node artifacts
node_modules
npm-debug.log*
yarn.lock
pnpm-lock.yaml
package-lock.json
# keep package*.json — they are copied explicitly in the Dockerfile

# build outputs (rebuilt inside the image)
dist
coverage

# vcs & tooling
.git
.gitignore
.vscode
.idea

# env & misc
.env
.env.*
*.tsbuildinfo
.DS_Store